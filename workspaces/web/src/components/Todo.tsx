import React from 'react';
import {Todo} from "@monorepo/nestjs/dist/src/todos/entities/todo.entity";

const Todo = props => {
  const [todo, setTodo] = useState<Todo>(initState);

  return <div>


  </div>;
}

export default Todo;
